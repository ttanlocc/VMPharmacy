---
phase: 01-transaction-safety
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/soft_delete_drugs.sql
  - hooks/useDrugs.ts
  - app/api/drugs/route.ts
  - app/api/orders/route.ts
  - types/database.ts
autonomous: true

must_haves:
  truths:
    - "Deleting a drug from the catalog sets deleted_at instead of removing the row"
    - "Soft-deleted drugs do not appear in drug search, drug list, or drug selection modals"
    - "Historical orders that reference a soft-deleted drug still display the drug name correctly"
    - "Order creation rejects soft-deleted drugs (treats them as non-existent)"
  artifacts:
    - path: "supabase/migrations/soft_delete_drugs.sql"
      provides: "Migration adding deleted_at column and partial index to drugs table"
      contains: "deleted_at"
    - path: "hooks/useDrugs.ts"
      provides: "Soft delete in deleteDrug, filtered fetch excluding deleted drugs"
      contains: "deleted_at"
    - path: "app/api/drugs/route.ts"
      provides: "GET handler filtering out soft-deleted drugs"
      contains: "deleted_at"
    - path: "types/database.ts"
      provides: "Updated drugs type with deleted_at field"
      contains: "deleted_at"
  key_links:
    - from: "hooks/useDrugs.ts"
      to: "supabase drugs table"
      via: "update deleted_at instead of .delete()"
      pattern: "\\.update\\(.*deleted_at"
    - from: "app/api/drugs/route.ts"
      to: "supabase drugs table"
      via: "filter deleted_at is null"
      pattern: "\\.is\\(['\"]deleted_at['\"],\\s*null\\)"
---

<objective>
Implement soft delete for drugs so that deleting a drug sets a `deleted_at` timestamp instead of removing the row. This preserves foreign key integrity with order_items and ensures historical orders display the original drug name.

Purpose: Prevents the "Thuoc" placeholder bug where deleted drugs show a generic fallback name in order history (SAFE-02).
Output: Migration SQL, updated hooks/API, updated TypeScript types.
</objective>

<execution_context>
@/home/loctran/.claude/get-shit-done/workflows/execute-plan.md
@/home/loctran/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@hooks/useDrugs.ts
@hooks/useTemplates.ts
@app/api/drugs/route.ts
@app/api/orders/route.ts
@types/database.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create soft delete migration and update TypeScript types</name>
  <files>supabase/migrations/soft_delete_drugs.sql, types/database.ts</files>
  <action>
1. Create `supabase/migrations/soft_delete_drugs.sql` with:
   ```sql
   -- Add soft delete column to drugs table
   ALTER TABLE drugs ADD COLUMN IF NOT EXISTS deleted_at TIMESTAMPTZ DEFAULT NULL;

   -- Partial index for efficient queries on active drugs
   CREATE INDEX IF NOT EXISTS idx_drugs_deleted_at ON drugs(deleted_at) WHERE deleted_at IS NULL;
   ```
   Include a comment that this should be run in Supabase SQL Editor.

2. Update `types/database.ts` to add `deleted_at` to the drugs table types:
   - In `drugs.Row`: add `deleted_at: string | null`
   - In `drugs.Insert`: add `deleted_at?: string | null`
   - In `drugs.Update`: add `deleted_at?: string | null`

   Follow the exact same pattern already used for templates (see templates table in the same file which already has `deleted_at`).
  </action>
  <verify>Run `npx tsc --noEmit` to confirm types are valid. Review SQL file for correct syntax.</verify>
  <done>Migration file exists. `drugs` type definition includes `deleted_at` field in Row, Insert, and Update.</done>
</task>

<task type="auto">
  <name>Task 2: Update hooks and API routes to use soft delete</name>
  <files>hooks/useDrugs.ts, app/api/drugs/route.ts, app/api/orders/route.ts</files>
  <action>
1. **hooks/useDrugs.ts** — Two changes:
   a. `fetchDrugs()`: Add `.is('deleted_at', null)` to the query chain (after `.select(...)`, before `.order('name')`). This mirrors the pattern in `useTemplates.ts` line 31.
   b. `deleteDrug()`: Change from hard delete to soft delete:
      ```typescript
      const deleteDrug = async (id: string) => {
          const { error } = await supabase
              .from('drugs')
              .update({ deleted_at: new Date().toISOString() })
              .eq('id', id);
          if (error) throw error;
          setDrugs(drugs.filter(d => d.id !== id));
      };
      ```
      The local state still filters it out for immediate UI update.

2. **app/api/drugs/route.ts** — In the GET handler, add `.is('deleted_at', null)` to the query so the server-side endpoint also excludes soft-deleted drugs. This ensures any consumer of this API (not just the hook) gets only active drugs.

3. **app/api/orders/route.ts** — In the POST handler's drug validation query (around line 124-127), add `.is('deleted_at', null)` to the `.from('drugs').select('id').in('id', drugIds)` query. This ensures soft-deleted drugs are treated as non-existent when creating orders, preventing orders that reference deleted drugs.

Do NOT modify the GET handler in orders/route.ts — historical orders should still JOIN to drugs (including soft-deleted ones) to show the original drug name. The drugs row still exists (just marked deleted), so the existing JOIN `drugs (name, unit, image_url)` in the orders GET query will continue to return the drug name correctly.
  </action>
  <verify>Run `npx tsc --noEmit` to confirm no TypeScript errors. Grep for `.delete().eq('id'` in useDrugs.ts to confirm hard delete is gone. Grep for `deleted_at` in useDrugs.ts, drugs/route.ts, and orders/route.ts to confirm all three files filter correctly.</verify>
  <done>deleteDrug uses soft delete. Drug listings exclude soft-deleted rows. Order creation rejects soft-deleted drugs. Order history still shows drug names for soft-deleted drugs via JOIN. TypeScript compiles.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. `hooks/useDrugs.ts` uses `.update({ deleted_at: ... })` not `.delete()`
3. `hooks/useDrugs.ts` fetchDrugs filters `.is('deleted_at', null)`
4. `app/api/drugs/route.ts` GET filters `.is('deleted_at', null)`
5. `app/api/orders/route.ts` POST drug validation filters `.is('deleted_at', null)`
6. `app/api/orders/route.ts` GET is unchanged (still JOINs drugs for history display)
</verification>

<success_criteria>
- Drugs are soft-deleted (row preserved with deleted_at timestamp)
- Soft-deleted drugs invisible in all drug listings and selection UI
- Historical orders display original drug names (not "Thuoc" placeholder)
- Orders cannot be created with soft-deleted drugs
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/01-transaction-safety/01-02-SUMMARY.md`
</output>
